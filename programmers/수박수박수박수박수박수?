class Solution {
    public String solution(int n) {
        String answer = "";
        String pattern = "수박";
        answer = pattern.repeat(n/2);
        if(n % 2 != 0){
            answer += "수";
        }
        return answer;
    }
}
