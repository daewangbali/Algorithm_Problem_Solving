class Solution {
    public long solution(int price, int money, int count) {
        long answer = -1;
        long n = 0;
        for(int i=1 ; i<=count ; i++){
            n += (price * i);
        }
        answer = Math.max(0,n-money);
        return answer;
    }
}
